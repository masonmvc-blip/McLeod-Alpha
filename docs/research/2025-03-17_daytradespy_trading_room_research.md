# McLeod Alpha Research Report: 2025-03-17 Trading Room — Post 38934

## Executive Assessment

The canonical source records eleven March 21 `565` calls completed from `5.15`
to `5.57` for a claimed `$452` net, while a co-presenter described the same
opening sequence as `5.16` to `5.55`. A separate formal `565`-call OMG
completed `5.66` to `6.00` in one minute. A later attempted `564`-call order
was canceled unfilled. Nine March 21 `565` calls then entered at `6.25`; the
target was reduced from `6.50` to `6.35`, but the position remained open with
an explicit overnight fallback. The published `565`-call pick was modeled
`4.89` to `5.18`.

## Source and Lineage

- Canonical post `38934`; Vimeo `1066918242`, title `TR Mar 17`, `01:10:43`.
- Complete caption track `219967671`: 1,494 cues,
  `00:00:01.925-01:10:14.485`.
- Post `38923` embeds the exact same Vimeo and caption assets. It is resolved
  separately as a duplicate and contributes no additional trade observation.
- Both authenticated players remained paused at zero volume; no audio played.
- Tier C: `TRANSCRIPT_COMPLETE_VISUAL_UNAVAILABLE`.

| Time | Evidence | Classification |
| --- | --- | --- |
| 10:49-16:32 | Eleven Mar. 21 `565` calls `5.15` to `5.57`; claimed `$452` net. Co-presenter cited `5.16` to `5.55`. | completed trade with speaker-specific prices |
| 13:52-15:04 | Formal Mar. 21 `565`-call OMG `5.66` to `6.00`. | completed OMG |
| 29:56-30:51 | Nine `564` calls were attempted, then order canceled after price ran away. | unfilled idea |
| 31:55-end | Nine Mar. 21 `565` calls `6.25`; exit revised `6.50` to `6.35`; held overnight if unfilled. | open carry |
| 54:54-56:27 | Published Mar. 21 `565` calls modeled `4.89` to `5.18`. | modeled pick |

The source reports profitable opening executions but finishes with unresolved
overnight exposure after the underlying weakened. The two opening exit prices
belong to different presenters and are retained rather than forced into one
fill. No independent ledger, executable paths, Greeks, MFE/MAE, fees, or
visual review exists.

`RESEARCH_ONLY_NO_LIVE_BEHAVIOR_CHANGE`.
